# Reminder Service ConfigMap
# Task: P5-T-102
apiVersion: v1
kind: ConfigMap
metadata:
  name: reminder-service-config
  labels:
    app: reminder-service
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  PUBSUB_NAME: "pubsub"
  SCHEDULER_INTERVAL_MS: "30000"
