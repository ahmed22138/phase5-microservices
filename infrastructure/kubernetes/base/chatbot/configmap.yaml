# Chatbot ConfigMap
# Task: P5-T-100
apiVersion: v1
kind: ConfigMap
metadata:
  name: chatbot-config
  labels:
    app: chatbot
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  PUBSUB_NAME: "pubsub"
