# Recurrence Service ConfigMap
# Task: P5-T-103
apiVersion: v1
kind: ConfigMap
metadata:
  name: recurrence-service-config
  labels:
    app: recurrence-service
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  PUBSUB_NAME: "pubsub"
  SCHEDULER_INTERVAL_MS: "60000"
